name: Android OCR Scanner Build

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      
    - name: 🔧 Fix gradlew permissions
      run: |
        echo "🔍 Vérification des permissions de gradlew..."
        ls -la gradlew || echo "gradlew non trouvé"
        
        # S'assurer que gradlew est exécutable
        if [ -f "gradlew" ]; then
          echo "✅ gradlew existe, modification des permissions..."
          chmod 755 gradlew
          ls -la gradlew
        else
          echo "❌ gradlew non trouvé, création..."
          # Télécharger gradlew
          wget -q https://raw.githubusercontent.com/gradle/gradle/master/gradlew
          chmod 755 gradlew
        fi
        
        # Vérifier les permissions
        echo "📋 Permissions finales :"
        ls -la gradlew
        echo ""
        echo "Test d'exécution :"
        ./gradlew --version || echo "Test échoué, continuation..."
        
    - name: ☕ Setup Java 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        
    - name: 🤖 Setup Android SDK
      uses: android-actions/setup-android@v3
      
    - name: 🏗️ Build APK
      run: |
        echo "🚀 Démarrage du build..."
        
        # Nettoyer
        ./gradlew clean || echo "Clean échoué, continuation..."
        
        # Build avec chemin absolu pour être sûr
        /bin/bash ./gradlew assembleDebug --stacktrace
        
        echo ""
        echo "🔍 Recherche de l'APK..."
        find . -name "*.apk" -type f 2>/dev/null || echo "Aucun APK trouvé"
        
    - name: 📦 Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: ocr-scanner-apk
        path: |
          app/build/outputs/apk/**/*debug*.apk
          app/build/**/*debug*.apk
          **/*debug*.apk
        if-no-files-found: warn
